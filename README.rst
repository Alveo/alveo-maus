MAUS
====

A python library to integrate the MAUS forced alignment system into Python.
MAUS is run via a docker container (stevecassidy/maus) and so should work on
any platform as long as Docker is installed.
